name: Keep Alive

on:
  schedule:
    - cron: '0 */8 * * *'

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Streamlit App
        run: |
          curl -L --max-time 60 -A "Mozilla/5.0" https://sem-cathode-analyzer.streamlit.app/
          echo "App pinged successfully"